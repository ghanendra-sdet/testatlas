<!--
LEARNING PATH PROPOSAL TEMPLATE
The FIRST document for a brand-new path — before the detailed architecture-proposal.md. This is the
issue/discussion pitch CONTRIBUTING.md requires ("open an issue or discussion first... explain the
learner problem, the proposed approach, alternatives considered, and maintenance implications") before
committing real time to a full architecture proposal. Lighter-weight, meant to get a fast go/no-go.
Delete this comment block before publishing. Replace every [bracketed] placeholder.
-->
# Learning Path Proposal: [Path Name]

## The Learner Problem

[What specific gap does this path fill? Who has this problem right now, and what do they currently have to do without TestAtlas covering it? Be concrete — not "testers should know X" but "a [specific role] facing [specific situation] currently has to [piece together from multiple unreliable sources / learn on the job with no structured resource / etc.]"]

## Proposed Scope (rough, not final)

[A rough sketch, NOT a full module list — that's what architecture-proposal.md is for, after this is approved. 3-5 sentences on what this path would cover and roughly how it's structured (sections, not modules).]

## Where This Fits

**Prerequisites**: [Which existing paths this builds on]
**Feeds into**: [Which future paths would reference this one's knowledge nodes]
**Position in `docs/architecture/CURRICULUM_ARCHITECTURE.md`**: [Which numbered path slot this fills, if the path is already listed there as planned]

## Alternatives Considered

[What else could address this learner problem instead of a new, full learning path? A section added to an existing path? A single module? A knowledge-node addition? State why a full path is the right scope, not the default assumption.]

## Relationship to Existing Knowledge Nodes

[Check `docs/architecture/KNOWLEDGE_GRAPH.md` — does this path's core content already have a home elsewhere that it should link to rather than duplicate? List any existing nodes this path would reference.]

## Maintenance Implications

[Rough estimate: how many modules, roughly what word-count total, how much of it needs domain expertise the current maintainer set may not have, whether it introduces a new fictional entity/fictional data convention or reuses AtlasBank.]

## Relationship to the Reference Curricula

[Does this path plan to follow `docs/architecture/REFERENCE_CURRICULUM_CERTIFICATION.md`'s defaults as-is, or does the learner problem genuinely call for a structural deviation? A rough answer is fine here — the full reasoning belongs in the architecture proposal, once this pitch is approved.]

## What Happens Next

If approved: a full `architecture-proposal.md` gets drafted, reviewed, and approved before any module content is written. This document's job is done once that next step is agreed to start.
